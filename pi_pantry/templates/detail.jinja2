{% extends "base.jinja2" %} 

{% block content %}
<ul>

        <h2>{{item.name}}</h2>
        <p>${{item.price}}</p>
        <h3>{{item.brand}}</h3>
        <p>{{item.category}}</p>
        <p>{{item.upc}}</p>
        <p>{{item.size}}</p>
        <p>{{item.description}}</p>
    </ul>

    {% endblock content %}