{% extends "base.jinja2" %} 

{% block content %}
<ul>
    {% for item in data %}
        <h2>{{item.name}}</h2>
        <p>${{item.price}}</p>
        <h3>{{item.brand}}</h3>
        <p>{{item.category}}</p>
        <p>{{item.upc}}</p>
        <p>{{item.size}}</p>
        <p>{{item.description}}</p>
        {% endfor %}
    </ul>

    {% endblock content %}