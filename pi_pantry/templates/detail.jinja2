{% extends "base.jinja2" %} 

{% block content %}
    <ul>
        <p>{{item.name}}</p>
        <p>{{item.brand}}</p>
        <p>{{item.size}}</p>
        <p>{{item.category}}</p>
        <p>{{item.upc}}</p>
        <p>{{item.description}}</p>
        <img src="{{item.image}}">
    </ul>
{% endblock content %}