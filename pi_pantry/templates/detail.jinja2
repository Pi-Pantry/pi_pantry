{% extends "base.jinja2" %}

{% block content %}
<ul>

        <h2>{{item.name}}</h2>
        <h3>{{item.brand}}</h3>
        <p>{{item.category}}</p>
        <p>{{item.upc}}</p>
        <p>{{item.description}}</p>
        <image src="{{item.image[2:-2]}}">
    </ul>

    {% endblock content %}
