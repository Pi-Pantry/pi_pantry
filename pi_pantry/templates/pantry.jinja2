{% extends "base.jinja2" %} 

{% block content %}
<ul>
    {% for item in data %}
    <h2>{{item.name}}</h2>
    <p>{{item.size}}</p>
    <a href="{{request.route_url('detail', upc=item.upc)}}">More Details</a>
    {% endfor %}
</ul>

{% endblock content %}